<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Category</title>
    <link rel="stylesheet" href="/style.css" />
    <script type="text/javascript" src="/Validation/validation.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f4f6f8;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .form-card {
            max-width: 500px;
            margin: 100px auto; 
            padding: 30px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .form-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(0, 0, 0, 0.15);
        }

        h3 {
            text-align: center;
            margin-bottom: 25px;
            font-weight: 600;
            color: #333;
        }

        .form-control {
            height: 45px;
            font-size: 16px;
            border-radius: 8px;
            transition: border 0.2s ease, box-shadow 0.2s ease;
        }

        .form-control:focus {
            border-color: #0d6efd;
            box-shadow: 0 0 8px rgba(13, 110, 253, 0.25);
            outline: none;
        }

        .button-group {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 25px;
            flex-wrap: wrap;
        }

        .button-group input {
            min-width: 150px;
            padding: 10px 15px;
            font-size: 16px;
            border-radius: 8px;
            transition: transform 0.2s ease, background 0.2s ease;
        }

        .button-group input:hover {
            transform: translateY(-2px);
        }

        .alert {
            margin-top: 15px;
            text-align: center;
            border-radius: 8px;
        }

        @media (max-width: 768px) {
            .form-card {
                margin: 80px 15px;
            }

            .button-group {
                flex-direction: column;
            }

            .button-group input {
                width: 100%;
            }
        }
    </style>
</head>

<body>

    <%- include("home.ejs") %>
    <div class="form-card">
        <h3>Add Category</h3>
        <form name="frm" action="/categories/add" method="post" onsubmit="return validateCategory();">
            <div class="mb-3">
                <input type="text" class="form-control" name="name" placeholder="Enter Category Name"
                    maxlength="30" required />
            </div>

            <div class="button-group">
                <input type="submit" class="btn btn-primary" value="Add New Category" />
                <input type="button" class="btn btn-secondary" value="Clear" onclick="document.frm.reset();" />
            </div>

            <% if(typeof msg !=="undefined" && msg.length>0) { %>
            <div class="alert alert-info">
                <%= msg %>
            </div>
            <% } %>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
